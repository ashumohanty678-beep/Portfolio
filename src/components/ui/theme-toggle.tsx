import React from 'react';

export interface ThemeToggleProps {
  className?: string;
  variant?: 'section' | 'floating' | 'header' | 'icon';
  showLabel?: boolean;
}

/**
 * ThemeToggle - disabled and removed for pure permanent dark mode
 */
export function ThemeToggle(_props: ThemeToggleProps) {
  return null;
}

export default ThemeToggle;
